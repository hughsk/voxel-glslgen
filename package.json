{
  "name": "voxel-glslgen",
  "version": "0.0.3",
  "description": "Push voxel terrain generation to the GPU using a GLSL shader",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "node_modules/.bin/browserify demo/index.js -o demo/bundle.js -d",
    "start": "npm run prepublish && node_modules/.bin/serve demo"
  },
  "keywords": [
    "glsl",
    "voxel",
    "generate",
    "terrain"
  ],
  "author": "Hugh Kennedy <hughskennedy@gmail.com> (http://hughskennedy)",
  "license": "MIT",
  "dependencies": {
    "three-copyshader": "0.0.1",
    "lut": "0.x.x"
  },
  "devDependencies": {
    "painterly-textures": "0.0.3",
    "voxel": "~0.1.5",
    "voxel-player": "0.0.1",
    "voxel-colorist": "0.0.1",
    "voxel-pp": "0.0.3",
    "voxel-engine": "~0.5.8"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/hughsk/voxel-glslgen.git"
  }
}
